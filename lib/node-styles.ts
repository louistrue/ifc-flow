// Define node styles for consistent use across the application
export const nodeStyle = {
  selected: {
    boxShadow: "0 0 0 2px rgba(59, 130, 246, 0.5), 0 4px 12px rgba(59, 130, 246, 0.3)",
    border: "none",
    borderRadius: "6px",
    zIndex: 10,
    transform: "scale(1.02)",
    transition: "all 0.2s ease-in-out",
  },
  default: {
    transition: "all 0.2s ease-in-out",
  },
}

